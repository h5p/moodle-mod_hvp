name: phplint
on: push
jobs:
  phplint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: overtrue/phplint@8.1
      with:
        path: .
        options: --exclude=*.log
